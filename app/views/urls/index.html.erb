<h1>Urls#index</h1>
<p>Find me in app/views/urls/index.html.erb</p>

<% @urls.each do |url| %>
  <ul>
    <li><%= url.name %> - <%= link_to url.short_url, url.long_url %> - <%= url.long_url %></li>
  </ul>

<% end %>

<%= link_to 'Create another link', new_url_path %>

